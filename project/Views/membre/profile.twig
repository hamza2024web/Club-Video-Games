<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GamersHub - Member Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
        
        body {
            font-family: 'Poppins', sans-serif;
        }
        
        .game-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        
        .game-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }

        .sidebar-item {
            position: relative;
            overflow: hidden;
        }
        
        .sidebar-item::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background-color: white;
            transition: width 0.3s ease;
        }
        
        .sidebar-item:hover::after {
            width: 100%;
        }

        .badge-pulse {
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.1);
            }
            100% {
                transform: scale(1);
            }
        }

        .tab-active {
            color: #6366f1;
            border-bottom: 2px solid #6366f1;
        }
        
        .custom-scrollbar::-webkit-scrollbar {
            width: 6px;
        }
        
        .custom-scrollbar::-webkit-scrollbar-track {
            background: #f1f1f1;
            border-radius: 10px;
        }
        
        .custom-scrollbar::-webkit-scrollbar-thumb {
            background: #8b5cf6;
            border-radius: 10px;
        }
        
        .achievement-card {
            transition: transform 0.3s ease;
        }
        
        .achievement-card:hover {
            transform: scale(1.03);
        }
    </style>
</head>
<body class="bg-gray-100">
    <div class="flex min-h-screen">
        <!-- Sidebar -->
        <aside class="w-64 bg-gradient-to-br from-indigo-900 via-purple-800 to-indigo-900 text-white min-h-screen p-6 relative">
            <div class="mb-8 text-center">
                <div class="flex justify-center mb-3">
                    <i class="fas fa-gamepad text-4xl text-purple-300"></i>
                </div>
                <h1 class="text-2xl font-bold">GamersHub</h1>
                <p class="text-sm text-purple-200">Member Portal</p>
            </div>

            <nav class="flex-grow">
                <ul class="space-y-1">
                    <li><a href="/member/dashboard" class="sidebar-item flex items-center p-3 hover:bg-indigo-700 rounded-lg transition"><i class="fas fa-home mr-3 text-purple-300"></i>Dashboard</a></li>
                    <li><a href="/member/tournaments" class="sidebar-item flex items-center p-3 hover:bg-indigo-700 rounded-lg transition"><i class="fas fa-trophy mr-3 text-purple-300"></i>Tournaments</a></li>
                    <li><a href="/member/events" class="sidebar-item flex items-center p-3 hover:bg-indigo-700 rounded-lg transition"><i class="fas fa-calendar-alt mr-3 text-purple-300"></i>Events</a></li>
                    <li><a href="/member/boutique" class="sidebar-item flex items-center p-3 hover:bg-indigo-700 rounded-lg transition"><i class="fas fa-store mr-3 w-5 sidebar-icon text-purple-300"></i>Boutique</a></li>
                    <li><a href="/member/games" class="sidebar-item flex items-center p-3 hover:bg-indigo-700 rounded-lg transition"><i class="fas fa-gamepad mr-3 text-purple-300"></i>My Games</a></li>
git                    <li><a href="/member/profile" class="sidebar-item flex items-center p-3 bg-indigo-700 rounded-lg transition"><i class="fas fa-user mr-3 text-purple-300"></i>Profile</a></li>
                </ul>
            </nav>

            <div class="absolute bottom-0 left-0 right-0 p-4">
                <div class="flex items-center space-x-2 mb-3 p-2 rounded-lg bg-indigo-800/50">
                    <img src="/{{profile.profile_photo}}" class="rounded-full border-2 border-purple-300 h-20 w-20" alt="Member Profile">
                    <div>
                        <p class="text-sm font-medium">{{profile.name}}</p>
                        <div class="flex items-center">
                            <i class="fas fa-circle text-green-500 mr-1 text-xs"></i>
                            <p class="text-xs text-purple-300">Online</p>
                        </div>
                    </div>
                </div>
                <!-- Mini Account Balance -->
                <div class="p-3 border-t border-indigo-800 sidebar-footer">
                    <div class="bg-indigo-800 rounded-lg p-2 text-center">
                        <div class="text-xs">Solde: <span class="font-bold">{{my_solde}} €</span></div>
                        <button class="mt-1 text-xs px-2 py-0.5 bg-purple-500 hover:bg-purple-600 rounded-full transition duration-200">
                            Recharger
                        </button>
                    </div>
                </div>
                <a href="/logout">
                    <button class="w-full bg-pink-600 hover:bg-pink-700 text-white py-2 rounded-lg transition flex items-center justify-center">
                        <i class="fas fa-sign-out-alt mr-2"></i>
                        Logout
                    </button>
                </a>
            </div>
        </aside>

        <!-- Main Content -->   
        <main class="flex-1 p-8">
            <!-- Header -->
            <header class="flex justify-between items-center mb-6">
                <div>
                    <h1 class="text-2xl font-bold text-gray-800">My Profile</h1>
                    <p class="text-gray-500">Manage your profile and gaming preferences</p>
                </div>
            </header>

            <!-- Profile Header with About Section Side-by-Side -->
            <section class="bg-white rounded-xl shadow-lg overflow-hidden mb-8 border border-gray-100">
                <div class="flex flex-col lg:flex-row">
                    <!-- Profile Photo Area with cleaner styling -->
                    <div class="lg:w-1/3 relative">
                        <div class="h-64 lg:h-full bg-gradient-to-br from-blue-600 to-indigo-600 relative overflow-hidden">
                            <img src="/{{profile.profile_photo}}" alt="Profile Photo" class="w-full h-full object-cover">
                            
                            <!-- Gradient overlay -->
                            <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent flex items-end p-6">
                                <div class="flex items-center">
                                    <div class="relative">
                                        <img src="/{{profile.profile_photo}}" alt="Profile Image" class="w-20 h-20 rounded-full border-4 border-white shadow-lg object-cover">
                                        <button class="absolute bottom-0 right-0 bg-white p-1.5 rounded-full shadow-lg hover:bg-gray-100 transition-transform duration-300">
                                            <i class="fas fa-camera text-gray-600 text-xs"></i>
                                        </button>
                                    </div>
                                    <div class="ml-4 text-white">
                                        <h2 class="text-lg font-bold">{{profile.name}}</h2>
                                        <p class="text-sm text-white/90">
                                            @{{profile.tag_name}}
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- About Section - Side by Side with Profile -->
                    <div class="lg:w-2/3 p-8">
                        <!-- Content -->
                        <div>
                            <!-- Status Row with enhanced status indicator -->
                            <div class="flex justify-between items-center mb-6">
                                <div class="flex items-center space-x-3">
                                    <span class="bg-green-100 px-3 py-1.5 rounded-full text-xs text-green-800 font-medium flex items-center shadow-sm border border-green-200/50">
                                        <span class="relative flex h-2 w-2 mr-2">
                                            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"></span>
                                            <span class="relative inline-flex rounded-full h-2 w-2 bg-green-500"></span>
                                        </span>
                                        Online
                                    </span>
                                </div>
                                <button class="px-5 py-2.5 bg-indigo-600 text-white text-sm rounded-lg flex items-center hover:bg-indigo-700 transition transform hover:-translate-y-0.5 hover:shadow-md">
                                    <i class="fas fa-edit mr-2"></i> Edit Profile
                                </button>
                            </div>

                            <!-- About Content with enhanced typography -->
                            <div>
                                <h3 class="text-lg font-bold text-gray-900 mb-3 flex items-center">
                                    <div class="w-8 h-8 rounded-lg bg-indigo-600 flex items-center justify-center shadow-sm mr-2">
                                        <i class="fas fa-user text-white text-sm"></i>
                                    </div>
                                    About
                                </h3>
                                
                                <!-- About text with cleaner styling -->
                                <div class="relative mb-6 pb-3">
                                    <div class="absolute left-0 top-0 h-full w-1 bg-indigo-400 rounded-full"></div>
                                    <div class="pl-4 bg-gray-50 p-3 rounded-r-lg border-l-0 border border-gray-200">
                                        <p class="text-gray-600 italic leading-relaxed">
                                            {{profile.about_me}}
                                        </p>
                                    </div>
                                </div>
                                
                                <!-- Personal info in a 2x2 grid with cleaner cards -->
                                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mb-5">
                                    <div class="flex items-start p-3 bg-white rounded-lg border border-gray-200 hover:shadow-md transition duration-300 transform hover:-translate-y-0.5">
                                        <div class="bg-blue-600 rounded-lg p-2.5 flex-shrink-0 shadow-sm">
                                            <i class="fas fa-map-marker-alt text-white"></i>
                                        </div>
                                        <div class="ml-3 pt-1">
                                            <p class="font-medium text-sm text-gray-800">Location</p>
                                            <p class="text-sm text-gray-500">{{profile.location}}</p>
                                        </div>
                                    </div>
                                    
                                    <div class="flex items-start p-3 bg-white rounded-lg border border-gray-200 hover:shadow-md transition duration-300 transform hover:-translate-y-0.5">
                                        <div class="bg-indigo-600 rounded-lg p-2.5 flex-shrink-0 shadow-sm">
                                            <i class="fas fa-birthday-cake text-white"></i>
                                        </div>
                                        <div class="ml-3 pt-1">
                                            <p class="font-medium text-sm text-gray-800">Birthday</p>
                                            <p class="text-sm text-gray-500">{{profile.date_naissance}}</p>
                                        </div>
                                    </div>
                                    
                                    <div class="flex items-start p-3 bg-white rounded-lg border border-gray-200 hover:shadow-md transition duration-300 transform hover:-translate-y-0.5">
                                        <div class="bg-blue-600 rounded-lg p-2.5 flex-shrink-0 shadow-sm">
                                            <i class="fas fa-calendar-day text-white"></i>
                                        </div>
                                        <div class="ml-3 pt-1">
                                            <p class="font-medium text-sm text-gray-800">Member Since</p>
                                            <p class="text-sm text-gray-500">January 15, 2025</p>
                                        </div>
                                    </div>
                                    
                                    <div class="flex items-start p-3 bg-white rounded-lg border border-gray-200 hover:shadow-md transition duration-300 transform hover:-translate-y-0.5">
                                        <div class="bg-indigo-600 rounded-lg p-2.5 flex-shrink-0 shadow-sm">
                                            <i class="fas fa-clock text-white"></i>
                                        </div>
                                        <div class="ml-3 pt-1">
                                            <p class="font-medium text-sm text-gray-800">Last Online</p>
                                            <p class="text-sm text-gray-500">Today at 2:45 PM</p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Recent Activity Section - Full Width Below -->
            <section class="bg-white rounded-xl shadow-sm overflow-hidden">
                <!-- Header with gradient background -->
                <div class="bg-gradient-to-r from-indigo-50 to-purple-50 px-6 py-4">
                    <h3 class="text-lg font-bold text-gray-900 flex items-center">
                        <div class="bg-gradient-to-r from-purple-600 to-indigo-600 w-8 h-8 rounded-lg flex items-center justify-center mr-3 shadow-sm">
                            <i class="fas fa-stream text-white"></i>
                        </div>
                        Recent Activity
                    </h3>
                </div>
                
                <!-- Activity feed with improved styling -->
                <div class="p-6">
                    <div class="custom-scrollbar max-h-[400px] overflow-y-auto pr-1">
                        <!-- Horizontal timeline with activities -->
                        <div class="relative">
                            <!-- Timeline line -->
                            <div class="absolute left-5 top-0 h-full w-0.5 bg-gradient-to-b from-purple-300 via-blue-300 to-indigo-300 rounded-full"></div>
                            
                            <!-- Activities -->
                            <div class="space-y-6">
                                {% for activity in activities %}
                                    <!-- Single activity with hover effect -->
                                    <div class="flex items-start relative hover:bg-purple-50 p-3 rounded-lg transition-all duration-200 group">
                                        <!-- Activity icon with animation -->
                                        <div class="flex-shrink-0 z-10 transform group-hover:scale-110 transition-all duration-300">
                                            {% if activity.type == 'tournament_started' %}
                                                <div class="w-10 h-10 rounded-full bg-gradient-to-br from-blue-400 to-blue-600 flex items-center justify-center shadow-md">
                                                    <i class="fas fa-trophy text-white"></i>
                                                </div>
                                            {% elseif activity.type == 'reimbursement' %}
                                                <div class="w-10 h-10 rounded-full bg-gradient-to-br from-green-400 to-green-600 flex items-center justify-center shadow-md">
                                                    <i class="fas fa-coins text-white"></i>
                                                </div>
                                            {% elseif activity.type == 'event_cancellation' %}
                                                <div class="w-10 h-10 rounded-full bg-gradient-to-br from-red-400 to-red-600 flex items-center justify-center shadow-md">
                                                    <i class="fas fa-calendar-times text-white"></i>
                                                </div>
                                            {% else %}
                                                <div class="w-10 h-10 rounded-full bg-gradient-to-br from-gray-400 to-gray-600 flex items-center justify-center shadow-md">
                                                    <i class="fas fa-bell text-white"></i>
                                                </div>
                                            {% endif %}
                                        </div>
                                        
                                        <!-- Activity content with improved typography and spacing -->
                                        <div class="ml-6 flex-1">
                                            <div class="flex flex-col sm:flex-row sm:items-center justify-between">
                                                <p class="text-sm text-gray-800 font-medium leading-relaxed">
                                                    {{ activity.message }}
                                                </p>
                                                <span class="text-xs text-gray-500 flex items-center sm:ml-4 mt-1 sm:mt-0 whitespace-nowrap flex-shrink-0">
                                                    <i class="far fa-clock mr-1.5 text-purple-400"></i>
                                                    {{ activity.created_at }}
                                                </span>
                                            </div>
                                            <div class="mt-2">
                                                {% if activity.type == 'tournament_started' %}
                                                    <div class="px-2.5 py-1 bg-gradient-to-r from-blue-100 to-blue-200 text-blue-800 text-xs inline-flex items-center font-medium rounded-full border border-blue-200 shadow-sm">
                                                        <i class="fas fa-trophy mr-1 text-blue-500"></i> Tournament
                                                    </div>
                                                {% elseif activity.type == 'reimbursement' %}
                                                    <div class="px-2.5 py-1 bg-gradient-to-r from-green-100 to-green-200 text-green-800 text-xs inline-flex items-center font-medium rounded-full border border-green-200 shadow-sm">
                                                        <i class="fas fa-coins mr-1 text-green-500"></i> {{ activity.points|default('+' ~ activity.amount) }}
                                                    </div>
                                                    <div class="mt-2 flex flex-wrap gap-2">
                                                        <button class="px-3 py-1 bg-green-600 text-white text-xs font-medium rounded-lg hover:bg-green-700 transition-all">
                                                            Transaction Details
                                                        </button>
                                                    </div>
                                                {% elseif activity.type == 'event_cancellation' %}
                                                    <div class="px-2.5 py-1 bg-gradient-to-r from-red-100 to-red-200 text-red-800 text-xs inline-flex items-center font-medium rounded-full border border-red-200 shadow-sm">
                                                        <i class="fas fa-calendar-times mr-1 text-red-500"></i> Cancelled
                                                    </div>
                                                    <div class="mt-2 flex flex-wrap gap-2">
                                                        <button class="px-3 py-1 bg-purple-600 text-white text-xs font-medium rounded-lg hover:bg-purple-700 transition-all">
                                                            Find Alternatives
                                                        </button>
                                                    </div>
                                                {% endif %}
                                            </div>
                                        </div>
                                    </div>
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                    
                    <!-- View all button with improved styling -->
                    <button class="mt-4 w-full px-4 py-2.5 bg-gradient-to-r from-purple-50 to-indigo-50 hover:from-purple-100 hover:to-indigo-100 text-gray-700 text-sm rounded-lg transition-all duration-300 flex items-center justify-center border border-purple-200 hover:shadow-md group">
                        <i class="fas fa-history mr-2 text-purple-500 group-hover:animate-spin"></i> View All Activity
                    </button>
                </div>
            </section>

            <!-- Optional: Additional Content Sections -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mt-8">
                <!-- Gaming Platforms -->
                <section class="bg-white rounded-xl shadow-sm p-6">
                    <h4 class="text-md font-semibold text-gray-900 mb-4 flex items-center">
                        <i class="fas fa-gamepad mr-2 text-purple-500"></i>
                        Gaming Platforms
                    </h4>
                    <div class="flex flex-wrap gap-3">
                        <div class="flex items-center px-3 py-1.5 bg-gradient-to-r from-blue-50 to-blue-100 rounded-lg text-gray-700 text-sm border border-blue-200 hover:shadow-md transition-all duration-200 hover:-translate-y-0.5">
                            <i class="fab fa-windows mr-2 text-blue-500"></i> PC
                        </div>
                        <div class="flex items-center px-3 py-1.5 bg-gradient-to-r from-purple-50 to-purple-100 rounded-lg text-gray-700 text-sm border border-purple-200 hover:shadow-md transition-all duration-200 hover:-translate-y-0.5">
                            <i class="fab fa-playstation mr-2 text-blue-600"></i> PS5
                        </div>
                        <div class="flex items-center px-3 py-1.5 bg-gradient-to-r from-gray-50 to-gray-100 rounded-lg text-gray-700 text-sm border border-gray-200 hover:shadow-md transition-all duration-200 hover:-translate-y-0.5">
                            <i class="fab fa-steam mr-2 text-gray-700"></i> Steam
                        </div>
                    </div>
                </section>
                
                <!-- Social Media -->
                <section class="bg-white rounded-xl shadow-sm p-6">
                    <h4 class="text-md font-semibold text-gray-900 mb-4 flex items-center">
                        <i class="fas fa-share-alt mr-2 text-purple-500"></i>
                        Social Media
                    </h4>
                    <div class="flex space-x-4">
                        <a href="{{profile.youtube}}" class="p-3 bg-gradient-to-br from-red-50 to-red-100 rounded-lg text-red-500 hover:shadow-lg transition-all duration-300 hover:-translate-y-1 border border-red-200 group">
                            <i class="fab fa-youtube group-hover:animate-pulse"></i>
                        </a>
                        <a href="{{profile.instagram}}" class="p-3 bg-gradient-to-br from-pink-50 to-pink-100 rounded-lg text-pink-500 hover:shadow-lg transition-all duration-300 hover:-translate-y-1 border border-pink-200 group">
                            <i class="fab fa-instagram group-hover:animate-pulse"></i>
                        </a>
                        <a href="{{profile.twitch}}" class="p-3 bg-gradient-to-br from-purple-50 to-purple-100 rounded-lg text-purple-500 hover:shadow-lg transition-all duration-300 hover:-translate-y-1 border border-purple-200 group">
                            <i class="fab fa-twitch group-hover:animate-pulse"></i>
                        </a>
                        <a href="{{profile.discord}}" class="p-3 bg-gradient-to-br from-indigo-50 to-indigo-100 rounded-lg text-indigo-500 hover:shadow-lg transition-all duration-300 hover:-translate-y-1 border border-indigo-200 group">
                            <i class="fab fa-discord group-hover:animate-pulse"></i>
                        </a>
                    </div>
                </section>
            </div>
        </main>
    </div>

    <!-- Edit Profile Modal -->
    <div id="editProfileModal" class="fixed inset-0 z-50 hidden overflow-y-auto">
        <div class="flex items-center justify-center min-h-screen p-4">
            <div class="relative bg-white rounded-xl shadow-xl max-w-2xl w-full mx-auto modal-content transform transition-all">
                <!-- Modal Header -->
                <div class="flex justify-between items-center p-6 border-b border-gray-200">
                    <h3 class="text-lg font-bold text-gray-900">Edit Profile</h3>
                    <button type="button" class="text-gray-400 hover:text-gray-500" id="closeModal">
                        <i class="fas fa-times text-xl"></i>
                    </button>
                </div>
                
                <!-- Modal Body -->
                <div class="p-6">
                    <form id="editProfileForm" class="space-y-6" action="/member/profile/edit" method="POST" enctype="multipart/form-data">
                        <!-- Profile & Cover Photo -->
                        <div class="space-y-4">
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-2">Cover Photo</label>
                                <div class="mt-1 flex flex-col items-center">
                                    <!-- Cover Photo Preview -->
                                    <div id="coverPhotoPreview" class="w-full h-32 bg-gray-100 rounded-lg mb-2 overflow-hidden hidden">
                                        <img id="coverPreviewImg" src="/{{profile.cover_photo}}" alt="Cover Photo Preview" class="w-full h-full object-cover">
                                    </div>
                                    <!-- Upload Area -->
                                    <div class="w-full flex justify-center px-6 pt-5 pb-6 border-2 border-gray-300 border-dashed rounded-lg">
                                        <div class="space-y-1 text-center">
                                            <i class="fas fa-image mx-auto h-12 w-12 text-gray-400"></i>
                                            <div class="flex text-sm text-gray-600">
                                                <label for="coverPhoto" class="relative cursor-pointer bg-white rounded-md font-medium text-purple-600 hover:text-purple-500 focus-within:outline-none">
                                                    <span>Upload a file</span>
                                                    <input id="coverPhoto" name="coverPhoto" type="file" class="sr-only" accept="image/*" onchange="previewCoverPhoto(this)">
                                                </label>
                                                <p class="pl-1">or drag and drop</p>
                                            </div>
                                            <p class="text-xs text-gray-500">PNG, JPG, GIF up to 10MB</p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-2">Profile Photo</label>
                                <div class="mt-1 flex items-center space-x-4">
                                    <div class="relative">
                                        <img id="profilePreviewImg" src="/{{profile.profile_photo}}" alt="Profile" class="w-24 h-24 rounded-full object-cover">
                                        <label for="profilePhoto" class="absolute bottom-0 right-0 bg-purple-600 text-white p-2 rounded-full cursor-pointer hover:bg-purple-700 transition">
                                            <i class="fas fa-camera"></i>
                                            <input id="profilePhoto" name="profilePhoto" type="file" class="hidden" accept="image/*" onchange="previewProfilePhoto(this)">
                                        </label>
                                    </div>
                                    <div class="text-sm text-gray-500">
                                        <p>Recommended: Square image, at least 400x400 pixels</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Basic Info -->
                        <div>
                            <h4 class="text-md font-semibold text-gray-900 mb-3">Basic Information</h4>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1">Username</label>
                                    <input type="text" name="username" value="{{profile.name}}" class="form-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none">
                                </div>
                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1">Display Name</label>
                                    <input type="text" name="displayName" value="{{profile.tag_name}}" class="form-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none">
                                </div>
                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                                    <input type="email" name="email" value="{{profile.email}}" class="form-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none">
                                </div>
                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1">Location</label>
                                    <input type="text" name="location" value="{{profile.location}}" class="form-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none">
                                </div>
                            </div>
                        </div>
                        
                        <!-- About -->
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">About Me</label>
                            <textarea name="about" rows="4" class="form-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none">{{profile.about_me}}</textarea>
                        </div>
                        
                        <!-- Social Links -->
                        <div>
                            <h4 class="text-md font-semibold text-gray-900 mb-3">Social Links</h4>
                            <div class="space-y-3">
                                <div>
                                    <label class="flex items-center text-sm font-medium text-gray-700 mb-1">
                                        <i class="fab fa-discord text-indigo-500 mr-2"></i> Discord
                                    </label>
                                    <input type="text" name="discord" value="{{profile.discord}}" class="form-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none" placeholder="Username#0000">
                                </div>
                                <div>
                                    <label class="flex items-center text-sm font-medium text-gray-700 mb-1">
                                        <i class="fab fa-instagram text-pink-500 mr-2"></i> Instagram
                                    </label>
                                    <input type="text" name="instagram" value="{{profile.instagram}}" class="form-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none" placeholder="@username">
                                </div>
                                <div>
                                    <label class="flex items-center text-sm font-medium text-gray-700 mb-1">
                                        <i class="fab fa-youtube text-red-500 mr-2"></i> YouTube
                                    </label>
                                    <input type="text" name="youtube" value="{{profile.youtube}}" class="form-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none" placeholder="@username">
                                </div>
                                <div>
                                    <label class="flex items-center text-sm font-medium text-gray-700 mb-1">
                                        <i class="fab fa-twitch text-purple-500 mr-2"></i> Twitch
                                    </label>
                                    <input type="text" name="twitch" value="{{profile.twitch}}" class="form-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none" placeholder="username">
                                </div>
                            </div>
                        </div>
                        <div class="bg-gray-50 px-6 py-4 border-t border-gray-200 rounded-b-xl flex justify-end">
                            <button type="button" class="px-4 py-2 bg-gray-200 text-gray-800 rounded-lg mr-2" id="cancelEditProfile">
                                Cancel
                            </button>
                            <button type="submit" class="px-4 py-2 bg-gradient-to-r from-purple-600 to-indigo-600 text-white rounded-lg hover:from-purple-700 hover:to-indigo-700 transition" id="saveProfile">
                                Save Changes
                            </button>
                        </div>
                    </form>
                    
                    <!-- Separate Password Change Form -->
                    <div class="mt-8 pt-6 border-t border-gray-200">
                        <form id="changePasswordForm" class="space-y-6" action="/member/password/change" method="POST">
                            <h4 class="text-md font-semibold text-gray-900 mb-3">Change Password</h4>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1">Current Password</label>
                                    <input type="password" name="currentPassword" class="form-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none" placeholder="Enter current password">
                                </div>
                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1">New Password</label>
                                    <input type="password" name="newPassword" class="form-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none" placeholder="Enter new password">
                                </div>
                            </div>
                            <div class="flex justify-end">
                                <button type="submit" class="px-4 py-2 bg-gradient-to-r from-purple-600 to-indigo-600 text-white rounded-lg hover:from-purple-700 hover:to-indigo-700 transition" id="changePassword">
                                    Update Password
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Modal functionality
        document.addEventListener('DOMContentLoaded', function() {
            const editProfileBtn = document.querySelector('button[class*="bg-gradient-to-r from-purple-600 to-indigo-600"]');
            const modal = document.getElementById('editProfileModal');
            const closeModal = document.getElementById('closeModal');
            const cancelBtn = document.getElementById('cancelEditProfile');
            const modalBackdrop = document.getElementById('modalBackdrop');
            
            // Open modal
            if (editProfileBtn) {
                editProfileBtn.addEventListener('click', function() {
                    modal.classList.remove('hidden');
                    document.body.classList.add('overflow-hidden');
                    
                    // Animate in
                    setTimeout(() => {
                        document.querySelector('.modal-content').classList.add('scale-100');
                        document.querySelector('.modal-content').classList.remove('scale-95');
                    }, 10);
                });
            }
            
            // Close modal functions
            function closeModalFunc() {
                document.querySelector('.modal-content').classList.add('scale-95');
                document.querySelector('.modal-content').classList.remove('scale-100');
                
                setTimeout(() => {
                    modal.classList.add('hidden');
                    document.body.classList.remove('overflow-hidden');
                }, 300);
            }
            
            if (closeModal) closeModal.addEventListener('click', closeModalFunc);
            if (cancelBtn) cancelBtn.addEventListener('click', closeModalFunc);
            if (modalBackdrop) modalBackdrop.addEventListener('click', closeModalFunc);
            
            // Toggle checkbox styling
            const toggleCheckboxes = document.querySelectorAll('.toggle-checkbox');
            toggleCheckboxes.forEach(checkbox => {
                checkbox.addEventListener('change', function() {
                    const label = this.nextElementSibling;
                    if (this.checked) {
                        label.classList.add('bg-purple-600');
                        label.classList.remove('bg-gray-300');
                    } else {
                        label.classList.remove('bg-purple-600');
                        label.classList.add('bg-gray-300');
                    }
                });
                
                // Initialize state
                const label = checkbox.nextElementSibling;
                if (checkbox.checked) {
                    label.classList.add('bg-purple-600');
                    label.classList.remove('bg-gray-300');
                }
            });
        });
        function previewCoverPhoto(input) {
            if (input.files && input.files[0]) {
                const reader = new FileReader();
                
                reader.onload = function(e) {
                    document.getElementById('coverPreviewImg').src = e.target.result;
                    document.getElementById('coverPhotoPreview').classList.remove('hidden');
                }
                
                reader.readAsDataURL(input.files[0]);
            }
        }
        
        function previewProfilePhoto(input) {
            if (input.files && input.files[0]) {
                const reader = new FileReader();
                
                reader.onload = function(e) {
                    document.getElementById('profilePreviewImg').src = e.target.result;
                }
                
                reader.readAsDataURL(input.files[0]);
            }
        }
        document.addEventListener("DOMContentLoaded", function() {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.has('password_updated')) {
                Swal.fire({
                    title: "Success!",
                    text: "Password updated successfully!",
                    icon: "success",
                    confirmButtonText: "OK",
                    timer: 3000 
                }).then(()=> {
                    window.location.href = "/member/profile";
                });

            }
        });
        document.addEventListener("DOMContentLoaded", function () {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.has('current_password_incorrect')){
                Swal.fire({
                icon: "error",
                title: "Oops...",
                text: "The Current Password Is Incorrect",
                confirmButtonText: "OK",
                timer: 3000 
                }).then(() => {
                    window.location.href = "/member/profile";
                });
            }
        });
        document.addEventListener("DOMContentLoaded", function() {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.has('profile_updated_successfully')) {
                Swal.fire({
                    title: "Success!",
                    text: "Profile updated successfully!",
                    icon: "success",
                    confirmButtonText: "OK",
                    timer: 3000 
                }).then(()=> {
                    window.location.href = "/member/profile";
                });

            }
        });
    </script>
    
    <style>
        .toggle-checkbox:checked {
            right: 0;
            border-color: #8b5cf6;
        }
        .toggle-checkbox:checked + .toggle-label {
            background-color: #8b5cf6;
        }
    </style>
</body>
</html>